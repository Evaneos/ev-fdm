#!/bin/bash
# Execute gulp and check if newly generated files had been pushed into the commit
#

echo 'Generating dist files'
gulp
DIFF=$(git diff --shortstat dist/)
if [ "$DIFF" != "" ]; then
	echo 'New dist files have been generated. Please commit them before pushing your changes.'
    exit 1
fi
exit 0